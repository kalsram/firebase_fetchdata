import 'package:flutter/material.dart';

class View_report extends StatelessWidget {
  const View_report({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:
      Center(
        child: Text('no reports'),
      )
    );
  }
}